# Production template — set these values in Render dashboard (do NOT commit real secrets)
# Render sets PORT automatically — do not override it there.

MONGODB_URI=
JWT_SECRET=
NODE_ENV=production
FRONTEND_URL=
